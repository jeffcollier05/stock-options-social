/**
 * Represents a request to decline a friend request.
 */
export class DeclineFriendRequestRequest {
    /**
     * The user ID of the requester whose friend request is being declined.
     */
    requesterUserId: string = '';
}
